# 任務清單：seq-tagging-span-export-metrics

> **Apply 前硬閘**：先執行 `~/Library/pnpm/openspec validate seq-tagging-span-export-metrics --type change`（或等價 non-strict all-changes command）與 `scripts/check-sdd.sh`，分別回報 OpenSpec schema validation 與 Project SDD lint。兩者通過後必須停止，取得使用者明確確認才可進入 Stage 1 `/opsx:apply`。主 session／team lead 是唯一可驗證 Red／Green evidence 與更新 checkbox 的角色；QA 與實作 agent 皆不得自行勾選。

> **裁決前置（NON-CHECKBOX）**：`design.md` 的 D1、D2、D3、D5 已於 2026-09-08 由維護者裁定（見 `design.md` 之「裁決紀錄與待決事項」），D1 取新增 `IAA_UNCALIBRATED_TYPES`、D2 只改 `sequence_tagging`、D3 不在本 change 做匯出對話框 UI、D5 之孤兒常數併入下一個以 015 為正典的 change 清除且 `SINGLE` 退場，四項皆與草案一致故 delta 無需修改。**D4 亦已於同日裁定**：推導模組落在 `design/prototype/pages/shared/`（沿用 `annotation-history.js` 的跨頁共用先例），確切檔名於 apply 時依該目錄命名慣例決定。五項皆已裁定，無任務因裁決而 blocked。

## 1. PR-SEQ-STATS-SPAN — 統計總覽改以 span 為母體

> **相依與平行性**：本群組嚴格序列 1.1 → 1.2 → 1.3；不使用 parallel markers。1.1 的 committed Red 必須先於 1.2。本群組不動 quality tab，也不動匯出流程。

**故事目標**：SC-004 — 讓 `sequence_tagging` 的 stats 區塊在 `tokenization` 與 `tagging_scheme` 兩個設定欄位消失後仍可正確渲染，母體由 token 序列改為已提交的 span 集合。

- [x] 1.1 修改 `design/prototype/tests/dataset/dataset-analysis-detail-stats-i18n.spec.ts`，新增對應 AC-2.7 的 Red 斷言：標籤類型分佈只出現不帶前綴的 label、一段 n 字標記計為 1 筆、片段長度分佈以字元分桶；先提交此單檔再執行，expected failure 必須來自現行 partial 仍以 token 與 tag 前綴渲染，並保存 command、exit 與失敗訊息。證據：commit `89cab2cd`（前置 `80c85699`／`58076cba`／`17272e6f`），單檔 `design/prototype/tests/dataset/dataset-analysis-detail-stats-i18n.spec.ts`；`PW_PORT=8971 corepack pnpm playwright test tests/dataset/dataset-analysis-detail-stats-i18n.spec.ts` 得 8 failed / 3 passed，失敗集合為 `:86` `:100` `:107` `:129` `:153` `:185` `:205` `:242` 八條 AC-2.7 斷言，通過的 `:21` `:45` `:65` 為其他 task type 對照組，證明失敗來自 partial 仍以 token 與 tag 前綴渲染而非測試檔本身損壞；`git diff --stat origin/main 89cab2cd -- design/prototype/pages openspec` 無輸出，確認此 Red 提交點尚未動任何產品檔。 [@senior-qa]
- [x] 1.2 Green：改寫 `design/prototype/pages/dataset/dataset-analysis-detail.partials/stats-sequence_tagging.html`，依 FR-009L 將三項指標改為標籤類型分佈、每句平均標記片段數與字元長度分佈；QA 已提交的斷言為契約，實作端只實作、維持原樣。證據：commit `86003683`（2 產品檔 `stats-sequence_tagging.html` 與 `dataset-analysis-detail.html`，合計 +79/−24）。partial 由單一 section 擴為三個 section：`statsSeqTagDistTitle`（標籤類型分佈，列出 `ORG`／`TITLE` 兩個不帶 `B-`／`I-` 前綴的 label）、新增 `statsSeqTagAvgSpanTitle`（每句平均標記片段數）與 `statsSeqTagSpanLenTitle`（依 `end − start` 字元長度分桶，4 字元以上併為 `4+`）；原 `O`／`B-PER`／`I-PER`／`B-ORG` 四列與 `sequenceTaggingStats.tags.*` 鍵於改版後已不復存在。`dataset-analysis-detail.html` 內嵌之 zh（`:710` 起）與 en（`:1065` 起）兩份 i18n bundle 同步改寫，`sequenceTaggingQuality` 一字未動（屬 FR-012L／群組 2）。`git diff 89cab2cd 86003683 -- design/prototype/tests/` 為空（0 行），確認實作端未改寫既有測試契約。主 session 獨立複跑 `PW_PORT=8995 corepack pnpm playwright test tests/dataset/dataset-analysis-detail-stats-i18n.spec.ts` → **11 passed (3.8s)、exit 0**。因變更落在 `design/prototype/pages`，另依 INVENTORY_FRESHNESS 以 `node scripts/gen-screen-inventory.mjs` 重生 `design/system/screen-inventory.md` 並單獨提交為 `ffddf70e`。 [@senior-frontend]
- [x] 1.2b 同步退役 `design/prototype/tests/dataset/dataset-analysis-detail-sequence-tagging-i18n.spec.ts` 之 `:15` 與 `:25` 兩條 stats 分頁測試中鎖死舊文案的斷言（`#statsSeqTagDistTitle` 的 `標記類型分佈`／`Tag Type Distribution`、`#statsSeqTagDistDesc` 的 token 與 O tag 語句），改寫為 FR-009L `:95` 生效後的 label-type 文案；本檔的 quality 分頁斷言（`:37` 起）屬 FR-012L 範圍，留待群組 2 處理，本任務不得更動。**計畫遺漏補記**：1.1 的契約只涵蓋新測試檔，未涵蓋既有迴歸測試對同一組 i18n key 的精確文案斷言，而 FR-009L `:91` 明文 BREAKING 廢止該文案，故 1.2 完成後此二測試必然失敗；CI 的 `Prototype — Playwright Tests` 執行 `pnpm test`（全量 `playwright test`），不同步退役即無法合併本群組 PR。驗證：`PW_PORT=8993 corepack pnpm playwright test tests/dataset/` 全數通過。證據：commit `93d6047f`（單檔，+3/−3）——`:21`／`:22` 兩行改為 `標籤類型分佈` 與 `各標籤類型在已提交標記片段中的筆數與比例`，`:33` 改為 `Label Type Distribution`，三處皆維持 `toHaveText` 精確比對而未弱化為 `toContainText`。`:34` 的 `not.toContainText(/[標記類型分佈全體]/)` 刻意保留：該字元集自 `b8698f24` 起用於擋 zh 文案洩漏進 en 模式，改版後 `標籤類型分佈` 仍供給「標」「類」「型」「分」「佈」、`distNote` 的「標記片段」仍供給「記」，護欄依然有效，僅「全」「體」成為冗餘。主 session 獨立複跑 `PW_PORT=8994 corepack pnpm playwright test tests/dataset/` → **90 passed (25.1s)、exit 0**，涵蓋本檔 quality 分頁（`:37` 起）未被更動仍全綠。 [@senior-qa]
- [x] 1.3 執行 command-only 群組驗證：`pnpm typecheck`、`pnpm playwright test tests/dataset/dataset-analysis-detail-stats-i18n.spec.ts`、`scripts/check-sdd.sh`、`scripts/check-spec-artifacts.sh`、`git diff --check`；全部預期 exit `0`，並保存 1.1 的失敗提交雜湊與 1.2 的 exit-0 evidence。證據（主 session 於 `93d6047f` 上逐條實跑）：`corepack pnpm typecheck` exit 0；`PW_PORT=8995 corepack pnpm playwright test tests/dataset/dataset-analysis-detail-stats-i18n.spec.ts` → 11 passed、exit 0；另因 CI 之 `Prototype — Playwright Tests` 執行全量 `pnpm test`（`design/prototype/package.json:6` 定義為 `playwright test`、無路徑過濾），加跑 `PW_PORT=8994 corepack pnpm playwright test tests/dataset/` → 90 passed / 0 failed、exit 0；`scripts/check-sdd.sh` → `Project SDD lint: 0 error(s), 29 warning(s)`、exit 0；`scripts/check-spec-artifacts.sh` → `Spec artifact check passed`、exit 0；`git diff --check` 無輸出、exit 0。1.1 的失敗提交雜湊為 `89cab2cd`（8 failed / 3 passed），1.2 的 exit-0 提交雜湊為 `86003683`。本群組對 `origin/main` 之最終 diff 為 6 檔：產品檔 2（`stats-sequence_tagging.html`、`dataset-analysis-detail.html`）、測試檔 2、產生檔 1（`screen-inventory.md`）、`openspec/**` 1，符合 Principle X 之 5 檔／300 行門檻。 [@main]

## 2. PR-SEQ-QUALITY-UALPHA — 品質監控改 span-level 指標與未校準中性呈現

> **相依與平行性**：本群組嚴格序列 2.1 → 2.2 → 2.3 → 2.4 → 2.4b → 2.4c → 2.4b2 → 2.4d → 2.5；前置為群組 1 合併與 D1 已裁定。2.1 的 committed Red 必須先於 2.2，2.3 的 committed Red 必須先於 2.4。本群組不動 stats partial，也不動匯出流程。

**故事目標**：SC-035、SC-036、SC-006、SC-021、SC-026、SC-027 — 主指標改 Krippendorff u-α，逐樣本分歧度與標記員排名改 span F1，並讓未校準型別只顯示數值與排序而不做門檻判定。

- [x] 2.1 修改 `design/prototype/tests/dataset/dataset-analysis-detail-sequence-tagging-i18n.spec.ts`，新增對應 AC-3.7、AC-3.18、AC-3.13、AC-3.14 的 Red 斷言：主指標顯示 u-α 與中性「待實證校準」標示、頁面同時查無門檻數值與達標色彩、分歧度指標名為 `pairwise_f1`、排名指標名為 `f1_to_merged_reference`；先提交此單檔再執行，expected failure 必須來自現行 partial 仍渲染 Token-level Alpha 與門檻，並保存 command、exit 與失敗訊息。證據：commit `3a60ac9a`，單檔 `design/prototype/tests/dataset/dataset-analysis-detail-sequence-tagging-i18n.spec.ts`、+93/−8。被移除的 8 行是 quality 分頁鎖死舊文案的既有斷言，涉及 `#iaaMethodName`、`#seqTagMaskingNote`、`#sequenceTaggingGroupAvg`、`#consistencyNote` 四個元素，其中前二者已被新契約覆蓋，後二者留下的覆蓋缺口另立任務 2.1b 處理。主 session 獨立複跑 `PW_PORT=8998 corepack pnpm exec playwright test tests/dataset/dataset-analysis-detail-sequence-tagging-i18n.spec.ts` → **2 failed / 2 passed**；兩條失敗為 `:43` zh 與 `:111` en 兩支 quality 分頁測試，皆止於 `#iaaMethodName` 的 `toContainText(/u-α/i)`，received 為 `Token-level Krippendorff's Alpha`，證明失敗來自現行 partial 仍渲染 token 級主指標；通過的 `:21` 與 `:31` 為 stats 分頁對照組，證明測試檔本身未損壞。`git diff bcd7d8f4 3a60ac9a -- design/prototype/pages openspec/changes/seq-tagging-span-export-metrics/specs` 無輸出，確認此提交點零觸碰產品檔與 delta。 [@senior-qa]
- [x] 2.1b 修改 `design/prototype/tests/dataset/dataset-analysis-detail-sequence-tagging-i18n.spec.ts`，把任務 2.1 一併移除卻未改寫的兩條 i18n 精確文案斷言補回新契約版本：群體平均列 `#sequenceTaggingGroupAvg` 與一致度說明 `#consistencyNote` 於 zh 與 en 兩語言各自斷言新的 span 級 u-α 文案，並斷言整個品質分頁不得殘留無連字號的 `Token Alpha` 字樣與 O tag 遮罩敘述。此條記錄任務 2.1 之計畫遺漏：AC-3.7 的字面字串為連字號版 `Token-level Alpha`，抓不到群體平均列的無連字號寫法，而一致度說明位於排名區塊、不在 IAA 面板選取範圍內，故實作端可只換方法列而讓頁面上下文案自相矛盾仍全綠。驗證：`PW_PORT=8996 corepack pnpm playwright test tests/dataset/dataset-analysis-detail-sequence-tagging-i18n.spec.ts` 出現失敗，失敗原因為現行 partial 於這兩個元素仍渲染 token 級文案。證據：commit `54e489cf`，同一測試單檔、+40/−0（純追加、零刪除），`git diff 3a60ac9a 54e489cf -- design/prototype/pages` 無輸出。斷言落點為 zh `:95`–`:108` 與 en `:173`–`:186`，各含群體平均列的 u-α 正面斷言與無連字號 `Token Alpha` 負面斷言、一致度說明的 span 正面斷言與遮罩／mask 負面斷言，以及對整個 `#qualityReady` 容器的頁面級護欄，合計 12 條。因這 12 條排在任務 2.1 既有斷言之後、而 Playwright 首次斷言失敗即中止該支 test，上述 2 failed 的複跑並未執行到它們；主 session 另以一支臨時隔離 spec（未提交，複驗後刪除，`git status --porcelain` 無輸出）把 zh 全部 6 條與 en 具代表性的 3 條共 9 條各自拆成獨立 test，對現行 partial 執行 `PW_PORT=8999 corepack pnpm exec playwright test --retries=0` → **9 條全數失敗**，received 分別為 `群體平均 — Token Alpha: 0.78 ／ 平均速度: 44.0s／句`、`Group avg — Token Alpha: 0.78 / Avg speed: 44.0s/sent`、`序列標記以 O tag 遮罩後的 token 作為比較母體；邊界 high_divergence 樣本仍維持獨立旗標呈現。`、`For sequence tagging, O-tag-masked tokens form the comparison pool. Boundary-level high_divergence samples remain separate flags.`，逐條證明失敗來自 partial 的 token 級文案而非選取器寫錯。頁面級護欄另外涵蓋標記員風險表的 `<th>Token Alpha</th>` 欄名，任務 2.2 須一併換掉。主 session 並複跑整個目錄 `PW_PORT=8999 corepack pnpm exec playwright test tests/dataset/` → **89 passed / 2 failed**，失敗集合僅上述兩支，確認本輪契約未波及 dataset 目錄其餘測試。 [@senior-qa]
- [x] 2.1c 修改 `design/prototype/tests/dataset/dataset-analysis-detail-sequence-tagging-i18n.spec.ts`，於 zh 與 en 兩支 quality 分頁測試各補一條斷言：標記一致性偏離分析表的單位欄 `#consistencyUnitCol` 必須與同表說明 `#consistencyNote` 同為 span 級母體，不得殘留 zh 的「可比較 token 數」或 en 的 `Comparable Tokens` 字樣。此條記錄與任務 2.1b 同型別的第三處計畫遺漏：這兩個元素由同一組 i18n map 在同一函式內一起賦值（`dataset-analysis-detail.html:1571`–`:1574`），而任務 2.1b 只約束了說明句，實作端可只換說明句、讓同一張表的欄名與說明自相矛盾而仍全綠。驗證：對現行 partial 單獨執行這兩條斷言必須失敗，received 為 zh 的「可比較 token 數」與 en 的 `Comparable Tokens`。證據：commit `47b1cf4f`，同一測試單檔、+14/−0（純追加、零刪除），`git diff 3103d3ab 47b1cf4f -- design/prototype/pages openspec` 無輸出。斷言落點為 zh `:113`–`:115` 與 en `:198`–`:200`（各為選取器一行＋正負面斷言各一），註解分別在 `:110`–`:112` 與 `:195`–`:197`。正面斷言刻意用 `toContainText(/span/i)` 而非鎖死完整字串，因新文案的確切措辭屬任務 2.2 的決定，此處只鎖「必須是 span 級母體」這個契約；負面斷言則以精確字串鎖死舊文案。這兩條同樣排在任務 2.1／2.1b 的斷言之後、跑整檔不會執行到，主 session 獨立以臨時隔離 spec（未提交，複驗後刪除，`git status --porcelain` 無輸出）拆成 4 支獨立 test，執行 `PW_PORT=8990 corepack pnpm exec playwright test --retries=0` → **4 failed / 0 passed**，received 為 zh 的 `可比較 token 數` 與 en 的 `Comparable Tokens`，證明失敗來自現行表頭仍取用 `consistencyUnitHeader` 的 token 級值而非選取器寫錯。 [@senior-qa]
- [x] 2.2 Green：改寫 `design/prototype/pages/dataset/dataset-analysis-detail.partials/quality-sequence_tagging.html`，依 FR-012L、FR-035、FR-036 換指標，並依 FR-043 加上中性未校準標示；此 partial 內查無任何門檻數字與達標色彩，QA 契約維持原樣。證據：commit `222b7ce8`，2 產品檔（partial 與 `dataset-analysis-detail.html` 之 zh／en 兩份內嵌 i18n bundle）、合計 +41/−47。逐元素落點：`#iaaMethodName` 改為「Krippendorff 單位化 u-α」／`Krippendorff's Unitizing u-α`，`#iaaMethodSuffix` 改為「— 主要指標（span 為計算單位）」／`— Primary metric (span-level)`，`#lblSeqTagMetric` 改為 `u-α`，`#lblSeqTagCalc` 改為「以 span 為比較單位；u-α 以連續體長度加權處理未標記區段」，`#seqTagMaskingNote` 整段連同 `.iaa-threshold-row` 區塊與 `card-pass`／`pass` 三處達標色彩類別一併退場，改以 `#seqTagCalibrationNote` 承載「u-α 待實證校準：目前僅顯示點估計值與排序，尚未訂定達標門檻。」，其英文文案為 `u-α pending empirical calibration`，與 `free_text` 的 not applicable 語意用字相異，符合 FR-043 第 3 點的文案分離要求；風險表原本硬編而無 i18n 屬性的 `<th>Token Alpha</th>` 改掛 `qualityRisk.headerUAlpha` 並在 zh 與 en 兩份 bundle 各定義一次（`grep -c` 得 2）；`#sequenceTaggingGroupAvg`、`#consistencyUnitCol`、`#consistencyNote` 三處分別成為「群體平均 — u-α」「可比較 span 數」「以可對齊的 span 作為比較母體」，與 `entity_recognition` 同型別先例逐字一致；FR-035／FR-036 的兩條低一致度樣本說明與排名指標名改為 pairwise F1（strict）與「與合併聚合參考值 F1」。退役鍵之孤兒引用檢查：`/usr/bin/grep -rn` 於 `design/prototype` 全樹搜尋 `sequenceTaggingQuality` 的 threshold 與 maskingNote 兩鍵，exit `1`、零輸出。`git diff 96a68deb HEAD -- design/prototype/tests/` 為空（0 行），`git diff 96a68deb HEAD --name-only` 恰為 3 檔且不含 tasks.md，確認實作端未改寫測試契約亦未自行勾選。主 session 獨立複跑 `PW_PORT=8993 corepack pnpm exec playwright test tests/dataset/` → **91 passed (14.4s)、exit 0**。因變更落在 `design/prototype/pages`，依 INVENTORY_FRESHNESS 另以 `node scripts/gen-screen-inventory.mjs` 重生 `design/system/screen-inventory.md` 並單獨提交為 `67509e0e`（+1/−1）。 [@senior-frontend]
- [x] 2.3 修改 `design/prototype/tests/dataset/dataset-analysis-detail-registry-mirror.spec.ts`，新增對應 AC-3.9、AC-3.8 的 Red 斷言：達標徽章分母同時排除未校準與 gate-excluded 型別、排除後綴計數涵蓋兩集合、未校準型別的文案與 `free_text` 文案相異；先提交此單檔再執行，expected failure 必須來自現行分母只排除 `free_text`，並保存 command、exit 與失敗訊息。證據：commit `a0251821`（單一測試檔，+105/−0）與收緊回歸鎖的 `f7895d8b`（同檔，+19/−0）。主 session 獨立複跑兩輪 `corepack pnpm exec playwright test tests/dataset/dataset-analysis-detail-registry-mirror.spec.ts`（PW_PORT 分別為 8998 與 8999）→ 兩輪皆 **2 failed、6 passed、exit 1**；兩條失敗各自停在自身第一個斷言：一條期望分母 1 而得 2（未校準型別仍被計入），一條期望分母 0 而得 1（全排除任務因此無法進入 not_applicable），失敗來源正是現行分母只排除 `free_text`。`corepack pnpm typecheck` exit 0；`git diff 8e6ed92d HEAD -- design/prototype/pages` 與 `-- openspec` 皆為 0 行、`git status --porcelain` 為空，確認契約端未動任何產品檔或規格檔。對應 AC-3.8 的第三條測試今日即為綠燈——該型別文案已由任務 2.2 之 `222b7ce8` 落地，故它是回歸鎖而非失敗證據，此點如實記載；主 session 覆核後要求補齊條文四個子句中未鎖的兩項，`f7895d8b` 補上「點估計值確有渲染」的正面斷言與「該區塊不得出現 free_text 那句文案」的具名字串否定斷言，前者對應之標記位於 dataset-analysis-detail.partials 之 quality-sequence_tagging 檔第 12 行的 iaa-card-value，該任務的分數卡格線為單卡故取首個元素無歧義。強度複核：`IAA_GATE_EXCLUDED_TYPES` 在產品端只有兩處消費（第 1322 與 1882 行），兩處都只做計數、不驅動任何文案渲染，故「把未校準型別併入既有排除常數」這種實作可讓兩條失敗轉綠且無行為差異；FR-024A 已明訂需為獨立常數 `IAA_UNCALIBRATED_TYPES`，此約束改由任務 2.4 之派工與主 session 覆核把關，不以測試硬鎖內部常數名。 [@senior-qa]
- [x] 2.4 Green：修改 `design/prototype/pages/dataset/dataset-analysis-detail.html`，依 FR-013 與 FR-024A 把未校準型別排除於 `x/y` 分母並套用中性樣式，`free_text` 既有文案維持原樣。證據：commit `dae628d3`（1 產品檔，+10/−2）。獨立常數 `IAA_UNCALIBRATED_TYPES` 落在第 1136 行，緊接第 1132 行的 `IAA_GATE_EXCLUDED_TYPES` 之後而**未併入**該陣列——兩者排除理由不同（一為本質上不設閘門、一為門檻尚未實證校準），FR-024A 第 145 行要求兩集合各自獨立。主 session 全樹複驗 `IAA_GATE_EXCLUDED_TYPES` 與 `IAA_UNCALIBRATED_TYPES` 的參照，`design/` 下（排除測試）僅有該檔第 1132、1134、1136、1327、1889 行五處命中，確認無第三個消費端漏改：第 1327 行的 `computeCompositeBadge` 與第 1889 行品質分頁待完成徽章的計數皆已同時濾除兩個集合，故 `x/y` 分母與「N 型排除」後綴計數依 AC-3.9 涵蓋兩集合，全型別皆被排除時回傳 not_applicable 而非 0/0。「套用中性樣式」一款經複核已由任務 2.2（`222b7ce8`）之逐型別文案落地滿足，本任務未再動 partial 或 CSS；AC-3.8 測試對主要與次要區塊各斷言 pass 與 fail 樣式計數為 0 且通過，`free_text` 的既有文案字面亦未被觸碰。主 session 獨立複跑（另起埠口，非沿用實作者的埠）：`PW_PORT=8981 corepack pnpm exec playwright test tests/dataset/dataset-analysis-detail-registry-mirror.spec.ts` → **8 passed、exit 0**；`PW_PORT=8982 corepack pnpm exec playwright test tests/dataset` → **94 passed、exit 0**，既有的 composite badge 契約檔未被連帶打壞；`corepack pnpm typecheck` exit 0。範圍護欄：`git diff 1cbda451 HEAD -- design/prototype/tests/` 為 0 行、`git diff 1cbda451 HEAD -- openspec` 為 0 行、`git status --porcelain` 為空，確認實作端未改寫失敗契約亦未越界動規格。另依 INVENTORY_FRESHNESS 重生 `design/system/screen-inventory.md` 並單獨提交為 `db8d7678`（僅來源 commit 指標一行變動）。 [@senior-frontend]
- [x] 2.4b 修改 `design/prototype/tests/task-management/task-detail-sampling-edit.spec.ts`，新增對應 FR-012L 與 FR-043 §2 的 Red 斷言：以 T006 這個純 sequence_tagging 任務為標的，登錄的主指標須為 span 單位的 u-α、門檻欄位須為空、摘要列與編輯列皆不得出現 0.75 這個目標值；先提交此單檔再執行，expected failure 必須來自登錄仍是 token 級指標與 0.75 預設門檻，並保存 command、exit 與失敗訊息。。證據：commit `f556d6c6`（單一測試檔，+49/−0，兩條新測試分別自第 154、172 行起，各自為獨立的 `test()` 區塊而非接在既有通過測試之後——Playwright 首次斷言失敗即中止該測試，接在既有斷言後方的新斷言永遠不會被執行，不構成失敗證據）。`git diff d1876467 HEAD -- design/prototype/pages` 為 0 行、`-- openspec` 為 0 行，`git status --porcelain` 為空，確認契約端未動任何產品檔或規格檔。主 session 獨立複跑 `PW_PORT=8987 corepack pnpm exec playwright test tests/task-management/task-detail-sampling-edit.spec.ts` → **2 failed、5 passed、exit 1**；兩條各自停在自身第一個斷言：第 164 行 `expect(...).not.toContainText` 期望不含 `0.75` 而實得 `Token-level Alpha（O-tag 遮罩）（目標 IAA 0.75）`，第 184 行 `.iaa-override-input` 期望 0 而實得 1——兩者的失敗來源都是登錄表本身仍為 token 級指標與 0.75 預設門檻，而非選擇器或路由損壞（既有 5 條同檔測試仍通過）。引用複核：FR-012L 的作用句位於 `openspec/changes/seq-tagging-span-export-metrics/specs/dataset/017-dataset-analysis-detail/spec.md` 第 111 行（u-α 以 `span` 為計算單位登錄、門檻欄位 MUST 為空），BREAKING 廢止句於同檔第 113 行；FR-043 §2 的「不得偷渡預設門檻」於同檔第 70 行。T006 為純 sequence_tagging 單一輸出任務，定義於 `design/prototype/pages/task-management/task-detail.data.js` 第 333 行起，故摘要列與編輯列各只有一列、由單一登錄項驅動（兩條測試皆以 `toHaveCount(1)` 明文鎖住此前提）。強度殘留風險（誠實記錄）：兩條測試鎖的是登錄表所呈現的指標名稱與門檻欄位形狀，並未證明畫面上的數值真的是以 span 為單位算出的 u-α；在原型層沒有真實計算管線的前提下這是可接受的範圍，實際計算單位由 change ② 已落地的 span-level CompactAnswer 保證。 [@senior-qa]
- [x] 2.4c Green：修改 `design/prototype/pages/task-management/task-config.data.js`，把 sequence_tagging 的登錄主指標換成 span 單位的 u-α 並讓門檻欄位退場，其餘輸出型別維持原樣。證據：commit `e60c345b`（1 產品檔，+7/−1）。登錄項落在第 420 行，改為 `zh: 'Krippendorff 單位化 α（u-α，以 span 為計算單位）'`、`en: "Krippendorff's unitizing alpha (u-α, span-level unit)"`、`defaultThreshold: null`、`computeUnit: 'span'`、`computeNote: 'u-α 以連續體長度加權處理未標記區段'`，四項分別對應 FR-012L（`openspec/changes/seq-tagging-span-export-metrics/specs/dataset/017-dataset-analysis-detail/spec.md` 第 111 行）的主指標、計算單位、門檻欄位為空、計算前置規則四個子句。此項刻意**不**設 `notApplicable`——依 FR-043 §3（同檔第 73 行）未校準與 gate-excluded 兩集合 MUST NOT 共用文案或狀態值，設了會讓標籤變成 `free_text` 那句「不適用」的錯誤陳述。主 session 覆核：`rg -n 'IAA_THRESHOLD_TOKEN|Token-level Alpha|Token-level alpha' design/prototype/pages` → exit 1、零輸出，廢止識別名稱在產品頁面已無孤兒引用（spec 第 113 行的 BREAKING 廢止句）；`git diff 2cfd1dfd HEAD -- design/prototype/pages | grep '^[+-].*free_text'` 無命中，確認其餘七個登錄項（含 `free_text`）一字未動。實作者初稿的註解誤含字面 `Token-level Alpha` 且留下未填的 `v...` 佔位，兩者皆已於後續 commit `cfae7e86` 更正並重新掃描。另依 INVENTORY_FRESHNESS 重生 `design/system/screen-inventory.md`，單獨提交為 `8dbbf189`（與 `cfae7e86` 之後的 `875b8f11`）。 [@senior-frontend]
- [x] 2.4b2 Red 帳目拆分（無新增測試碼）：`design/prototype/tests/task-management/task-detail-sampling-edit.spec.ts` 中自第 172 行起的第二個 `test()` 區塊，為獨立於 2.4b 第一條之外的另一份失敗契約——它鎖的是編輯列不得渲染門檻覆寫輸入框（FR-043 §2），與第一條所鎖的摘要列指標文案分屬不同產出點、由不同產品檔滿足，故各自配對一條實作任務。本條不新增任何測試碼，證據沿用同一 commit `f556d6c6`；其失敗訊息為第 184 行 `.iaa-override-input` 期望 0 而實得 1，與 2.4b 第一條停在第 164 行的失敗互相獨立（Playwright 首次斷言失敗即中止該測試，兩條若合寫於同一區塊則第二份契約永遠不會被執行）。追記理由如實記載：原計畫把兩份契約併記於 2.4b 一條，實作階段才發現其 Green 端跨兩個產品檔，超出單一任務可表達的範圍。 [@senior-qa]
- [x] 2.4d Green：修改 `design/prototype/pages/task-management/task-detail.html`，讓門檻相關的三個產出點改以「該登錄項是否具備有限門檻值」為條件，指標標籤的不適用回退維持只看既有的不適用旗標。證據：commit `d6bf9636`（1 產品檔，+6/−2）。三處落點：第 5467 行 `getEffectiveTargetAgreement` 的守衛加上 `|| !Number.isFinite(entry.defaultThreshold)`；第 5502 行於摘要列既有的「不適用」與「帶目標值後綴」兩條路之間插入第三條 `else if (!Number.isFinite(entry.defaultThreshold))`，只印指標名稱、不帶 `（目標 IAA …）` 後綴（未新增 i18n key，故中英兩份既有字串語意不變）；第 5720 行決定是否產出 `.iaa-override-input` 的條件由 `!entry.notApplicable` 換成 `Number.isFinite(entry.defaultThreshold)`。第四個消費端 `getAppliedIaaMetrics`（第 8961 行）未被觸碰，其 `threshold` 經第一處改動自動變為 `null` 而 `metric` 仍為 u-α 名稱，符合 FR-043 §2「不得偷渡預設門檻」而不失去指標本身。`free_text` 的既有「不適用」行為完全不變——指標標籤回退（第 5461 行）與摘要列第一條分支（第 5501 行）仍只看 `notApplicable`。主 session 獨立複跑（另起埠口，非沿用實作者的 8971/8972/8973）：`corepack pnpm typecheck` exit 0；`PW_PORT=8966 corepack pnpm exec playwright test tests/task-management` → **317 passed、exit 0**（含失敗契約 `task-detail-sampling-edit.spec.ts` 的 7 條全綠）；`PW_PORT=8967 corepack pnpm exec playwright test tests/dataset` → **94 passed、exit 0**，群組 2 前段（2.2／2.4）落地的 dataset 端契約未被連帶打壞。範圍護欄：`git diff 2cfd1dfd HEAD -- design/prototype/tests` 為 0 行、`-- openspec` 為 0 行、`git status --porcelain` 為空，確認實作端未改寫失敗契約亦未越界動規格。另依 INVENTORY_FRESHNESS 重生 `design/system/screen-inventory.md`，單獨提交為 `0667b648`。 [@senior-frontend]
- [x] 2.5 執行 command-only 群組驗證：`pnpm typecheck`、`pnpm playwright test tests/dataset/`、`pnpm playwright test tests/task-management/`、`scripts/check-sdd.sh`、`scripts/check-spec-artifacts.sh`、`git diff --check`；全部預期 exit `0`，另以 `rg -n 'IAA_THRESHOLD_TOKEN|Token-level Alpha' design/prototype/pages` 預期 exit `1` 且無輸出，證明廢止常數在產品頁面已無孤兒引用（失敗契約 spec 以負面斷言保留該字串，故不納入掃描範圍）。證據：主 session 於 `6b2d5bcb` 這個樹狀態上逐項執行，七項全數符合預期。`corepack pnpm typecheck` exit 0；`PW_PORT=8951 corepack pnpm exec playwright test tests/dataset/` → **94 passed、exit 0**；`PW_PORT=8952 corepack pnpm exec playwright test tests/task-management/` → **317 passed、exit 0**（兩支各起獨立埠口，未沿用先前任一次跑測的埠）；`scripts/check-sdd.sh` → **0 error、31 warning、exit 0**；`scripts/check-spec-artifacts.sh` → `Spec artifact check passed`、exit 0；`git diff --check` exit 0；回歸掃描 `rg -n 'IAA_THRESHOLD_TOKEN|Token-level Alpha' design/prototype/pages` → **exit 1 且零輸出**。群組規模覆核：對 origin/main 的差異為 9 檔／397 插入／67 刪除，扣除測試檔、產生檔與 openspec 之後的產品檔為 4 個共 116 行，在 5 檔／300 行門檻內。 [@main]

## 3. PR-SEQ-EXPORT-DERIVATION — 匯出層 BIO 推導與 tokenizer metadata

> **相依與平行性**：本群組嚴格序列 3.1 → 3.2 → 3.3 → 3.4 → 3.5；前置為群組 2 合併、D3 與 D4 已裁定。3.1 的 committed Red 必須先於 3.2，3.3 的 committed Red 必須先於 3.4。D3 已裁定匯出對話框改由以 `specs/_archive/014-task-detail/spec.md` 為正典的 companion change 承載，該 companion 單為 **issue #742**。**裁決 H（2026-09-09，維護者）**：3.3 與 3.4 不整組移出，改為**拆兩半**——FR-042 的詞級推導屬 017 正典，其純函式層（對齊擴張、tokenizer metadata 必填檢查、擴張摘要的資料產出）留在本 change；AC-5.3 與 AC-5.4 中屬畫面的子句（擴張摘要的顯示與展開、阻擋匯出的提示、匯出檔 metadata 呈現）連同方案與單位選擇器一併移交 issue #742。理由是整組移出會讓 FR-042 於 archive 時寫進 017 正典卻無任何實作與測試，留下空頭條文。

**故事目標**：SC-033、SC-034 — 以決定性推導把 `spans[]` 轉為 BIO／BIOES／IOB2 序列，並在詞級模式下強制 tokenizer metadata 與對齊擴張報告。

- [x] 3.1 新增 `design/prototype/tests/dataset/dataset-analysis-detail-span-to-bio.spec.ts`，依 AC-5.1、AC-5.2 建立 Red 斷言：字元級 BIO 與 BIOES 序列逐格正確、空 span 樣本輸出等長全 `O`、重複匯出結果逐字元相同；先提交此單檔再執行，expected failure 必須來自推導模組尚未存在，並保存 command、exit 與失敗訊息。證據：commit `470a4920`，單檔 `design/prototype/tests/dataset/dataset-analysis-detail-span-to-bio.spec.ts`；`PW_PORT=8973 corepack pnpm playwright test tests/dataset/dataset-analysis-detail-span-to-bio.spec.ts` 得 7 failed、exit 1，七條皆止於 `page.addScriptTag: Error: Failed to load script at http://127.0.0.1:8973/pages/shared/span-tagging-export.js`，證明失敗來自推導模組尚未存在而非測試檔本身損壞；`git diff --stat origin/main HEAD -- design/prototype/pages openspec/changes/seq-tagging-span-export-metrics/specs` 於該提交點無輸出，確認未動任何產品檔與 delta。斷言強度：三種方案的 tag 陣列以字面列出而非由來源推算，逐格錯位會被抓到；`tokenizer` 以 `hasOwnProperty` 判定鍵不存在，因為值為 `undefined` 的鍵仍會序列化進匯出檔 metadata；決定性一項另讀模組原始碼確認查無 `Date.now`、`new Date`、`Math.random`、`navigator.language`、`toLocaleString`——同一瀏覽器內重複呼叫必然自我一致，抓不到時鐘、亂數與本機語系。 [@senior-qa]
- [x] 3.2 Green：依 D4 裁定的落點新增 span 轉序列推導模組，實作 FR-041 的三種方案；此模組為純函式，輸入僅限 `spans[]` 與原始文本，QA 契約維持原樣。證據：commit `2a7124af`，落點 `design/prototype/pages/shared/span-tagging-export.js`（D4 鎖定之目錄，沿用 annotation-history.js 先例），全域名 `window.LabelSuiteSpanTaggingExport`。同一條失敗契約重跑得 7 passed、exit 0，QA 契約一字未動。`token_unit` 為 `word` 時明確擲出例外而非退回字元級——metadata 宣稱 word 而內容是字元級的匯出檔比被拒絕的匯出更糟，FR-042 於任務 3.4 補上。另 commit `0f19022c` 重生 `design/system/screen-inventory.md`：新增原型產品檔會讓產生器的來源 commit 指標位移，首跑 `scripts/check-sdd.sh` 因此以 INVENTORY_FRESHNESS 亮一個 error，重生後為 `Project SDD lint: 0 error(s), 25 warning(s)`、exit 0，`scripts/check-spec-artifacts.sh` 為 `Spec artifact check passed`、exit 0。 [@senior-frontend]
- [x] 3.3 修改 `design/prototype/tests/dataset/dataset-analysis-detail-span-to-bio.spec.ts`，依 AC-5.3、AC-5.4 之資料層子句（裁決 H）補上詞級 Red 斷言：擴張筆數與逐筆內容正確、原始 span 於擴張後維持原值、缺 tokenizer 版本時推導回傳阻擋結果且不產生任何序列產物、字元級路徑不得帶 tokenizer metadata 亦不得產生擴張紀錄；先提交此單檔再執行，expected failure 必須來自詞級路徑尚未實作，並保存 command、exit 與失敗訊息。畫面子句（摘要顯示與展開、阻擋提示文案）依裁決 H 不在本任務斷言範圍，由 issue #742 承接。證據：commit `5d22cb0a`，單檔 `design/prototype/tests/dataset/dataset-analysis-detail-span-to-bio.spec.ts`；`PW_PORT=8973 corepack pnpm playwright test tests/dataset/dataset-analysis-detail-span-to-bio.spec.ts` 得 5 failed、7 passed、exit 1，新增五條位於 `:232 :273 :312 :338 :365`，五條皆止於 `Error: span-tagging-export: token_unit word is not implemented yet`，證明失敗來自詞級路徑尚未實作；原有七條字元級斷言在同次執行維持通過，確認任務 3.1 的契約未被本次改動削弱。斷言強度：詞級 tags 長度以 token 數而非字元數斷言，只記錄 `token_unit` 卻沒真的換單位的實作會被抓到；擴張紀錄逐筆整包比對而非只比筆數，因為 AC-5.3 要求可逐筆列出原始文字、擴張後文字與 offset 差值，只有計數無法滿足；阻擋結果同時斷言不帶 tags 與 expansions 鍵，避免呼叫端拿著「已阻擋卻仍有序列」的物件照樣匯出；字元級回退一項刻意仍帶著詞級選項呼叫，把「照抄收到的 options 進 metadata」這種走偏實作擋在本層。切詞邊界由呼叫端以 `tokens` 傳入而非由模組當場呼叫引擎，否則模組會取得環境相依輸入而違反 FR-041 第 2 點。 [@senior-qa]
- [x] 3.4 Green：依 FR-042 於推導模組補上詞級對齊擴張、tokenizer metadata 必填檢查與擴張摘要的資料產出；此模組維持純函式，不得自行讀取 DOM 或觸發匯出。依裁決 H，匯出流程的方案與單位選擇器接線不在本任務，移交 issue #742；QA 契約維持原樣。證據：commit `aabc8262`，單檔 `design/prototype/pages/shared/span-tagging-export.js`；同一條失敗契約重跑 `PW_PORT=8973 corepack pnpm playwright test tests/dataset/dataset-analysis-detail-span-to-bio.spec.ts` 得 12 passed、exit 0，QA 契約一字未動。設計要點：切詞邊界由呼叫端傳入而非模組當場呼叫引擎，模組因此仍是輸入的純函式，也守住 FR-041 第 2 點；tokenizer 欄位不齊時回傳 `{ blocked: true, reason }` 而非擲例外，因為被阻擋的匯出是呼叫端要呈現的預期結果而非程式錯誤，且該回傳不含任何序列產物，呼叫端無從據以寫檔；依 FR-042 第 5 點，字元級即使收到 tokenizer 與 tokens 也一律不寫入 tokenizer metadata、alignment mode 與擴張筆數。另 commit `dc47a76e` 重生 `design/system/screen-inventory.md`：產生器以最後一次動到原型頁面的 commit 為來源指標，本任務的功能提交本身就會讓該檔過期，故分開一次提交。重跑 `scripts/check-sdd.sh` 為 `Project SDD lint: 0 error(s), 24 warning(s)`、exit 0，`scripts/check-spec-artifacts.sh` 為 `Spec artifact check passed`、exit 0。 [@senior-frontend]
- [ ] 3.5 執行 command-only 群組驗證：`pnpm typecheck`、`pnpm playwright test tests/dataset/`、`scripts/check-sdd.sh`、`scripts/check-spec-artifacts.sh`、`git diff --check`；全部預期 exit `0`，並保存字元級與詞級各一組實際匯出樣本作為決定性 evidence。 [@main]

## 4. PR-SEQ-EXPORT-FINAL — 完整驗證、正典回寫與 archive readiness

> **相依與平行性**：前置為 3.5 通過與群組 1～3 全數合併；本群組只有 command-only verification 與 archive 寫作，不動原型程式。

**故事目標**：SC-033、SC-035 — 以四個獨立 gate 與 Source-Verify evidence 證明本 change 可回寫正典並封存。

- [ ] 4.1 執行 command-only final verification：`~/Library/pnpm/openspec validate seq-tagging-span-export-metrics --type change`、`scripts/check-sdd.sh`、`scripts/check-spec-artifacts.sh`、`scripts/pre-commit-tests.sh`、`pnpm playwright test`、`git diff --check`；全部預期 exit `0`，並分開記錄 OpenSpec schema、Project SDD lint、code/test 與 scope evidence。 [@main]
- [ ] 4.2 執行正典回寫前的 Source-Verify：逐條 `rg` 驗證 delta 引用的 FR、AC、SC、常數與檔案路徑皆可在正典或 delta 定位，並確認 `IAA_THRESHOLD_TOKEN` 在 `specs/` 與 `design/` 下只剩歷史 Changelog 記述、現行條文查無引用；`SEQUENCE_TAGGING_SCHEMES` 於 015 的孤兒宣告依 D5 裁定處理，其清除不屬本 change 的通過條件。 [@main]

## Pre-merge finalization（在 /opsx:apply 外，NON-CHECKBOX）

所有 apply checkbox 完成、PR-group review 順序與使用者確認均通過後，final PR group 才執行 `/opsx:archive seq-tagging-span-export-metrics`。Archive 必須把版本回寫為 017 v3.0.0 並新增 Changelog 條目，同時處理三件正典層的收尾：退役 SC-024（Token-level Alpha 遮罩）、新增 SC-033～SC-036、更新 `LowConsistencySampleEntry` 與 `AnnotatorQualityRankingEntry` 的指標名稱列舉。依 `docs/sdd-workflow.md` §6.2 逐條 grep 衍生視圖的 canonical citation；final merge 後才更新 `specs/STATUS.md`，並依 issue #578／#596 先例判斷正典 spec 是否留在 `specs/dataset/`。
